name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  merge_group:
  schedule:
    - cron: "0 6 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (${{ matrix.language }})
    runs-on: ubuntu-latest
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        include:
          - language: javascript-typescript
            build-mode: none
          - language: rust
            build-mode: manual

    steps:
      - name: Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@e8817f70ab72f0dbbe78007a8fe42b109e768598 # v3.26.8
        with:
          languages: ${{ matrix.language }}
          build-mode: ${{ matrix.build-mode }}
          queries: security-extended
          config-file: ./.github/codeql/codeql-config.yml

      - name: Install Rust toolchain
        if: matrix.language == 'rust'
        uses: dtolnay/rust-toolchain@stable # stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo
        if: matrix.language == 'rust'
        uses: Swatinem/rust-cache@23bde2884508604e0e681a6c568ecbd27dc00ab3 # v2.7.3
        with:
          workspaces: contracts/stellar-give

      - name: Build contract
        if: matrix.language == 'rust'
        working-directory: contracts/stellar-give
        run: cargo build --release --target wasm32-unknown-unknown

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@e8817f70ab72f0dbbe78007a8fe42b109e768598 # v3.26.8
        with:
          category: "/language:${{ matrix.language }}"
